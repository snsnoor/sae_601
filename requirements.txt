# Interface et logique
geopandas          # manipulation de données géographiques (cartes, GeoJSON)
pandas             # manipulation de données tabulaires
requests           # requêtes HTTP (APIs, téléchargements)
shapely>=2.0       # géométries (polygones, points)
streamlit          # interface web interactive
plotly             # graphiques interactifs (dashboard)
numpy              # calculs numériques (dashboard)

# Base de données
duckdb             # base analytique locale

# Réseau (retry/backoff pour dpe_api.py)
urllib3
